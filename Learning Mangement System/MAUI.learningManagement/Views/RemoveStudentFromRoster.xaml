<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUI.learningManagement.Views.RemoveStudentFromRoster"
             Title="RemoveStudentFromRoster">
    <VerticalStackLayout Spacing = "10" >
        <Label
            Text="Remove Student From Roster"
            FontSize="20"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Label
            Text = "Students"
            FontSize="18"
            Margin="0,10,0,0"/>

        <ListView ItemsSource = "{Binding Student}"
          SelectedItem="{Binding SelectedPerson, Mode=TwoWay}">
        </ListView>

        <Label
            Text = "Course"
            FontSize="18"
            Margin="0,10,0,0"/>

        <ListView ItemsSource = "{Binding Course}"
          SelectedItem="{Binding SelectedCourse, Mode=TwoWay}">
        </ListView>

        <Button
            Text = "Remove Student from course"
            Clicked="RemoveStudentClicked"/>

        <Button
            Text = "Back"
            Clicked="BackClicked"/>
    </VerticalStackLayout>
</ContentPage>